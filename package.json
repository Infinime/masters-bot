{
  "name": "masters-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "vitest": "2.1.8",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.2.2",
    "@cloudflare/kv-asset-handler": "^0.3.4",
    "base64-js": "^1.5.1"
  }
}
